import CatalogSection from "@/components/sections/CatalogSection";

export const metadata = { title: "جميع المنتجات — إيرتو" };

export default function ProductsPage() {
    return (
        <div style={{ paddingTop: "80px" }}>
            <CatalogSection />
        </div>
    );
}
